import { CountDigitPipe } from './count-digit.pipe';

describe('CountDigitPipe', () => {
  it('create an instance', () => {
    const pipe = new CountDigitPipe();
    expect(pipe).toBeTruthy();
  });
});
